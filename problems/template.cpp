#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 示例：输入两个整数并输出它们的和
    int a, b;
    cin >> a >> b;
    cout << a + b << endl;

    return 0;
}
